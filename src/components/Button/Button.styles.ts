import styled from "styled-components";
import tw from "twin.macro";

export const StyledButton = styled.button`
  ${tw `bg-indigo-500 block rounded-lg text-white text-center py-2 font-bold w-full`}
`