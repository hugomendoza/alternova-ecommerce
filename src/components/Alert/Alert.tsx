import { StyledAlert, StyledAlertText } from './Alert.styles'

export const Alert = () => {
  return (
    <StyledAlert>
      <StyledAlertText>Tu carrito esta vacio</StyledAlertText>
    </StyledAlert>
  )
}
